# To use your model in the Webapps/app.py Flask app, copy the best model file to the Webapps directory or update the path in app.py.
# Example command to copy the model file:
# cp ../best_model.joblib ./best_model.joblib
# Or update app.py to use the correct relative path.
